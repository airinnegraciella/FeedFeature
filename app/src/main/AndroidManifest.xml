<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.main">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning"
        android:usesCleartextTraffic="true">

        <activity android:name="com.example.main.LoginActivity"
            android:theme="@style/LoginTheme">
        </activity>

        <activity android:name="com.example.main.feature.feed.HomeActivity"
            android:label="Feeds">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.example.featurefeed.presentation.like.FeedLikeActivity"
            android:label="Likes">
        </activity>
        <activity android:name="com.example.featurefeed.presentation.comment.FeedCommentActivity"
            android:label="Comments">
        </activity>
        <activity android:name="com.example.featurefeed.presentation.create_feed.FeedCreateActivity"
            android:label="Create Post">
        </activity>
        <activity android:name="com.example.featurefeed.presentation.edit_comment.EditFeedCommentActivity"
            android:label="Edit Comment">
        </activity>
    </application>

</manifest>